// Ensure nested patient routes resolve consistently (Next / Turbopack).
export const dynamic = "force-dynamic";

export default function StaffClinicPatientsLayout({ children }) {
  return children;
}
