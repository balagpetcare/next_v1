"use client";

// Flat canonical URL for service content & media (see next.config redirect from nested .../services-pricing/services/:id/content).

import StaffServiceContentClient from "../../_components/StaffServiceContentClient";

export default function StaffServicePricingServiceContentPage() {
  return <StaffServiceContentClient />;
}
