import StaffStockRequestDetailClient from "../../_components/StaffStockRequestDetailClient";

/**
 * Staff stock request detail — canonical URL: .../inventory/stock-requests/[id].
 * UI: shared client (branch context, permissions, API).
 */
export default function StaffStockRequestDetailPage() {
  return <StaffStockRequestDetailClient />;
}